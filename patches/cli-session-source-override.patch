--- a/run_agent.py	2026-09-08 14:26:15
+++ b/run_agent.py	2026-09-08 14:26:16
@@ -501,7 +501,7 @@
         """Create session DB row on first use. Disables _session_db on failure."""
         if self._session_db_created or not self._session_db:
             return
-        source = self.platform or os.environ.get("HERMES_SESSION_SOURCE", "cli")
+        source = os.environ.get("HERMES_SESSION_SOURCE") or self.platform or "cli"
         try:
             self._session_db.create_session(
                 session_id=self.session_id,
--- a/agent/conversation_compression.py	2026-09-08 14:26:16
+++ b/agent/conversation_compression.py	2026-09-08 14:26:16
@@ -516,7 +516,7 @@
             agent._session_db_created = False
             agent._session_db.create_session(
                 session_id=agent.session_id,
-                source=agent.platform or os.environ.get("HERMES_SESSION_SOURCE", "cli"),
+                source=os.environ.get("HERMES_SESSION_SOURCE") or agent.platform or "cli",
                 model=agent.model,
                 model_config=agent._session_init_model_config,
                 parent_session_id=old_session_id,
